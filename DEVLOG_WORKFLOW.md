# VRAS - VR AVATAR STUDIO - 開発日記 作業手順

この文書を開発日記作成の正本とする。

## 0. 実行モード

通常運用と手動検証を分ける。

### 通常運用モード

- §1 から §12 まで連続実行する。
- 各セクションの正常終了ごとにユーザー確認を待たない。
- エラー、リトライ、判断が必要な例外が発生した場合は、その時点で内容と次の対応を報告する。
- 公開前ゲートを通過できない場合は commit / push へ進まない。

### セクション検証モード

ユーザーが「1セクションずつ」「結果を確認しながら」などと指定した場合に使用する。

- 1セクションだけ実行する。
- そのセクションの結果、エラー、リトライ、未完了事項を報告して停止する。
- ユーザーの「次へ」「続けて」などの指示を受けて次セクションへ進む。
- セクション内で必要な再試行や検証は、そのセクションを完了させるために実施してよい。

### 漫画試作モード

ユーザーが「今日の文で漫画だけ試す」「公開せず4コマを比較する」など、4コマの試作だけを明示した場合に使用する。

- §2〜§5のうち、必要な事実確認、既存記事の読込、台本作成、画像生成と画像QAだけを実施する。
- 試作物は `.devlog-work/YYYY-MM-DD/` 配下へ正式成果物と衝突しない名前で保存し、`docs/` の正式記事・正式画像・一覧・トップページを置き換えない。
- `status.json` の正式公開状態を試作のために変更しない。
- commit / push / GitHub Pages公開確認へ進まない。
- Discord投稿は、ユーザーがその試作について明示的に投稿を依頼した場合だけ行う。
- このモードは手動試作用であり、定期実行には使用しない。毎日03:00 JSTの定期実行は必ず「通常運用モード」として§1〜§12を連続実行し、公開ゲート通過後にサイト反映、commit / push、GitHub Pages公開確認まで完了させる。

## 1. 実行時刻と「1日」の定義

- 定期実行: 毎日 03:00 JST。
- 記事日付: 実行日の日本時間の日付を使う。
- 集計期間: 前日 03:00:00 JST から当日 02:59:59 JST まで。
- 例: 2026-07-26 03:00 に作る記事は `2026-07-26` とし、2026-07-25 03:00 から 2026-07-26 02:59:59 までの開発を扱う。
- 手動再実行でも同じ期間を使い、再実行時刻から24時間などの相対期間へ変えない。

この区切りにより、深夜まで続く開発を同じ日記へまとめ、03:00 時点で内容を確定できる。

## 2. 事実確認の正本

最優先の確認対象:

- ローカル開発リポジトリ: `H:\codexapp\vrm-avatar-studio`
- 原則ブランチ: `main`

必ず確認するもの:

1. 現在のブランチ
2. 集計終了時点の HEAD SHA とコミットメッセージ
3. 集計期間内のコミット一覧
4. 各主要コミットの変更規模
5. 未コミット差分の有無
6. 実際に確認できた不具合・改善内容

「集計終了時点の HEAD」は現在HEADをそのまま使わず、当日 02:59:59 JST 以前で最後のコミットを特定して使う。03:00以降に新しいコミットが存在する場合、それらは当日記事の集計対象へ含めない。

現在の未コミット差分は確認するが、集計期間内に作成・変更された事実を確認できない差分を、その日の実装実績として記事へ含めない。未コミット差分は既存作業を壊さないための状態確認にも使う。

GitHub は公開済み状態や push 後の確認に使う。記事本文の事実確認は、GitHub への反映待ちで遅れないようローカルリポジトリを正本とする。

### サイト側に前回の未コミット差分が残っている場合

作業開始時に `H:\codexapp\vr-avatar-viewer-site` の `git status --short` を確認する。

- clean なら通常の checkout で当日分を作る。
- 前回の日記など当日とは別の未コミット差分がある場合、その checkout を reset / clean / 削除 / 上書きしない。
- 代わりに DevSpace の isolated worktree を `main` の現在HEADから作り、当日分はその clean worktree で作業する。
- worktreeを作れない場合だけ未完了として停止する。

これにより、未完成の前日下書きを保存したまま次の日記を独立して作成できる。

## 3. 記事種別とテーマ決定

最初に、集計期間内で確認できた変更を前日の開発日記と比較し、当日の記事を次のどちらにするか決める。

```text
development      = 通常の開発進捗回
feature-spotlight = 閑話休題の機能紹介回
```

### 3.1 通常の開発進捗回にする条件

前日から読者へ新しい結果として説明できる「有意な進展」が1つ以上確認できる場合は `development` とする。

有意な進展の例:

1. 見た目や使い勝手が明確に変わった
2. 不具合が解消し、修正後の結果を確認できた
3. 新しい取込・変換・互換性・ファイル形式対応が実用段階へ進んだ
4. パフォーマンスが計測可能な形で改善した
5. 前日まで未成立だった機能や検証が成立した
6. 内部変更でも、利用者や今後の実装へ明確な新しい能力を追加した

通常回の主題は、集計期間の変更から読者に最も伝わりやすいものを1つ選ぶ。優先順位は次とする。

1. 見た目や使い勝手が大きく変わった機能
2. 大きな不具合の解消
3. 新しいファイル形式・取込・互換性対応
4. パフォーマンス改善
5. 内部整理・テスト・保守性改善

### 3.2 閑話休題の機能紹介回へ切り替える条件

前日と比べて開発状況が実質的に変わっておらず、新しい結果として説明できる有意な進展がない場合は、無理に進捗を記事化せず `feature-spotlight` へ切り替える。

次のような場合は、原則として「有意な進展なし」と判定する。

- 前日と同じ不具合・同じ機能の調整や検証が続いているだけ
- パラメータ調整、見た目比較、ログ追加、診断UI追加だけで、機能の到達状態が前日と変わっていない
- 調査・仮説検証・テスト再実行だけで、解決や新しい成立条件へ到達していない
- リファクタリングや整理だけで、読者へ新しい結果として説明できる変化がない
- 集計期間のコミットが0件で、未コミット差分も前日の作業の継続に留まり、新しい成果を確認できない

コミット件数だけでは判定しない。コミットが0件でも新しい結果が確認できれば通常回にでき、コミットが複数あっても内容が前日と実質同じなら機能紹介回へ切り替える。

### 3.3 機能紹介テーマの選び方

`feature-spotlight` では、VRAS - VR AVATAR STUDIO - の現在実装済み機能から、特徴が伝わりやすいものを1つ選ぶ。

選定ルール:

1. 現在のソースコードまたは現行仕様で実装済みと確認できる機能だけを扱う。
2. 未実装、構想段階、検証途中の機能を「既に使える機能」として紹介しない。
3. `docs/devlog/*.html` の過去記事を確認し、過去の機能紹介回と同じ機能・同じ切り口を繰り返さない。
4. 機能紹介回では記事へ次のメタ情報を入れ、後続回が機械的に重複確認できるようにする。

```html
<meta name="devlog:type" content="feature-spotlight">
<meta name="devlog:feature" content="<stable-feature-key>">
```

5. 通常回では次を入れる。

```html
<meta name="devlog:type" content="development">
```

6. 機能紹介回のタイトルは `閑話休題：` で始め、通常の進捗回と明確に区別する。
7. 前日の開発テーマを無理に引き継がず、「このアプリで何ができるか」が一読で分かる切り口を優先する。

記事の主題と4コマの主題は必ず同一にする。通常回のその他の変更は「ほかにも進んだこと」として後半で短く扱う。機能紹介回では、当日の小さな作業を主役に昇格させない。

## 4. 4コマ台本

事実確認と記事種別の決定が終わってから台本を作る。

4コマは記事本文の要約を4分割するのではなく、**その日の主題を使った短い漫画**として成立させる。技術的な事実は守りつつ、視点、役割、起伏、オチの種類を毎回変えてよい。

### 4.1 直近回との重複確認

台本を書く前に、原則として直近5回分の `image_prompt.txt` または公開済み4コマ内容を確認する。各回について少なくとも次を把握する。

- 1コマ目の始まり方（要望、困りごと、成功状態、勘違い、日常会話など）
- 中盤の展開（説明、実演、失敗、調査、逆転、暴走など）
- 主なボケ役 / ツッコミ役
- 4コマ目の締め方（技術まとめ、成功、皮肉、脱力、逆転、余韻など）

同じ型の使用自体は禁止しない。ただし、直前回とほぼ同じ起承転結になる場合は別案を優先し、直近5回で同型が多い場合も意識的に別の展開へ寄せる。

### 4.2 台本候補を複数考えてから採用する

当日の主題から、内部的に少なくとも3案の展開候補を考える。3案は説明文の言い換えではなく、**物語構造そのもの**を変える。

採用時の優先順位:

1. 技術的事実を誤解させない
2. 当日の主題が一目で分かる
3. 直近回と展開が重複しにくい
4. 4コマ目に漫画としての反応、意外性、余韻のいずれかがある
5. 技術説明だけで4コマを使い切らない

不採用案を記事や画像プロンプトへ残す必要はない。

### 4.3 展開パターンの例

以下は候補集であり、順番固定のテンプレートではない。内容に合うものを選び、組み合わせたり変形してよい。

- **王道開発型**: 要望 → 実装 → 問題 / 発見 → 解決やオチ
- **期待と現実型**: 簡単そう / 良さそう → 実際に触る → 想定外 → 一言オチ
- **原因勘違い型**: 原因Aを疑う → Bも疑う → 真因判明 → 脱力 / 納得
- **直したら増えた型**: 1つ直す → 別の問題が見える → さらに増える → 開発あるあるの締め
- **仕様膨張型**: 小さな要望 → 「ついでに」 → 機能が育つ → タスク量との落差
- **逆転型**: 自信満々な側が説明 → 一言の確認 → 前提が崩れる → 役割逆転
- **ろひ暴走型**: 完了報告 → ろひの追加アイデア → 連鎖的に広がる → ルミナ側がツッコミ
- **ルミナ暴走型**: 技術的に盛る → ろひが用途を確認 → 過剰実装に気づく → 軌道修正
- **Unity / 設定擬人化型**: 実在する挙動や既定値をキャラクター的に表現 → 人間側と噛み合わない → 方針変更
- **成功したのに型**: きれいに成功 → 喜ぶ → 別角度から見る → 小さな次課題や一言オチ
- **何も起きない型**: 大苦戦を覚悟 → あっさり成立 → 疑って再確認 → 「ほんとに？」で締める
- **伏線回収型**: 1コマ目の何気ない台詞 / 小物 → 中盤では別件に見える → 4コマ目で意味が返ってくる
- **別視点型**: バグ、旧設定、アバター、GPU、コンバータ等の視点から始め、人間側へ戻す。擬人化しても技術的事実そのものは変えない
- **日常系型**: 大事件を作らず、開発中の小さな違和感や会話の間で笑わせる
- **機能寸劇型**: `feature-spotlight` で、機能説明そのものではなく「その機能が必要になる状況」を寸劇にして見せる

### 4.4 役割とオチを固定しない

- ろひを毎回ツッコミ役、ルミナを毎回説明役に固定しない。
- 二人が同時に勘違いしてもよいし、二人が協力してUnityや旧設定の挙動に振り回されてもよい。
- 4コマ目は「ルミナが技術的にまとめて終わり」に限定しない。ろひの一言、無言の表情、逆転、皮肉、安心、脱力、次の欲望などで締めてもよい。
- 4コマすべてに技術説明台詞を置く必要はない。絵で分かる部分は会話を日常寄り・感情寄りにしてよい。
- 「今日は○○を実装しました」「これで○○できます」の説明だけを順番に並べるより、登場人物がその変化にどう反応したかを優先する。

### 4.5 記事種別ごとの事実制約

`development` では、通常の「要望 → 実装 → 問題 → 解決」も有効な選択肢として残す。ただし当日の事実がその流れを支持するときに使い、毎回の既定形にはしない。

`feature-spotlight` では、機能を今日実装したように見せない。既存機能を使った寸劇、利用シーン、意外な使い方、困りごとの解消などから展開を作る。

共通ルール:

- 通常回の技術内容は実際の当日開発を基にする。
- 機能紹介回の技術内容は現在実装済みと確認できた機能事実を基にする。
- 会話・リアクション・比喩・擬人化の誇張は可。
- 実際に起きていない重大事故、データ消失、公開事故などを事実のように描かない。
- 擬人化やギャグのために、実在しない機能、性能値、障害原因を作らない。
- 4コマだけ別テーマにしない。

## 5. 4コマ画像

画像生成ルールの固定部分は `DEVLOG_IMAGE_PROMPT_BASE.md` を唯一の正本とする。`DEVLOG_WORKFLOW.md` へ固定プロンプト本文を重複記載しない。

画像生成時は必ず、次の2要素から1本の完成プロンプトを組み立てる。

1. `DEVLOG_IMAGE_PROMPT_BASE.md` の全文
2. §2〜§4の事実確認・記事種別・台本から作る `TODAY'S DEVLOG CONTENT` 可変ブロック

完成プロンプトは **固定プロンプト全文 + 当日可変ブロック** の順で連結する。画像生成内容を過去会話や前日の生成指示から補完しない。

### 当日可変ブロックに必ず含めるもの

- 当日の4コマタイトル
- Panel 1〜4それぞれのScene
- Panel 1〜4それぞれのDialogue（発話者と台詞）
- 各PanelのVisual tone / Mood
- `DEVLOG TYPE: development` または `DEVLOG TYPE: feature-spotlight`
- 通常回では、当日の実装事実を列挙した `FACTUAL DEVELOPMENT THEMES`
- 機能紹介回では、現行実装で確認できた機能事実を列挙した `FACTUAL FEATURE THEMES`
- 前日のテーマや過去の機能紹介テーマなど、混入すると誤生成につながるものを必要に応じて列挙した `TODAY'S EXCLUDED TOPICS`
- 当日の内容で特に誤解されやすい表現がある場合の明示的な禁止事項

可変ブロックには、事実確認で確認できなかった機能・数値・事故を追加しない。機能紹介回では「今日実装した」「今日完成した」など、その日の新規進捗と誤認させる表現を入れない。

### 画像生成前の必須手順

1. `DEVLOG_IMAGE_PROMPT_BASE.md` をその都度読み込む。記憶や前回のコピーで代用しない。
2. §2〜§4の結果から当日可変ブロックを新規作成する。前日の可変ブロックを編集して流用しない。
3. 固定プロンプト全文と当日可変ブロックを連結し、今回だけの完成プロンプトを作る。
4. 完成プロンプト内のタイトル、4コマ内容、台詞、事実テーマが当日分だけになっていることを確認する。
5. `H:\codexapp\vrm-avatar-studio\Docs\Branding\rohi.png` を DevSpace の `read_image` 等で実画像として読み込む。ファイル存在確認だけでは参照完了としない。
6. 完成プロンプトを画像生成の直前に明示し、その完成プロンプトだけを今回の生成内容として扱う。
7. 正規の `image_gen.text2im` を使用して4コマを生成する。

`image_gen` のAPI上 `prompt: null` 等の形式が要求される場合でも、過去会話から内容を推測させてよいという意味ではない。**呼び出し直前に、その日の完成プロンプト全文を生成指示として明示しておくことを必須**とする。

Discord投稿、commit / push、公開確認など「画像生成後に行う作業」は完成プロンプトへ混ぜない。後続作業の指示が会話上に存在していても、それを4コマの描画内容として扱わない。

### 固定プロンプトの変更

次のような毎日共通の変更は `DEVLOG_IMAGE_PROMPT_BASE.md` だけを修正する。

- ろひの正式参照方法
- ルミナの固定デザイン
- 4コマのページ構成
- 吹き出しルール
- スマートフォン可読性
- 色温度・基本画調
- 共通の禁止UI / メタ表現
- 生成後の共通QA項目

当日のタイトル、Scene、Dialogue、実装テーマは固定プロンプトへ追加しない。

### 画像生成呼び出しの必須チェック

- ChatGPT の画像生成機能を使い、SVG、HTML、CSS、Canvas などで代替画像を作らない。
- 画像生成には正規の `image_gen.text2im` を使用する。別名・無効なツール経路・未確認の画像生成ツールを使わない。
- `DEVLOG_IMAGE_PROMPT_BASE.md` と当日可変ブロックの両方が揃っていない状態で画像生成を開始しない。
- ろひの実画像を視覚参照できていない状態で「参考画像を使用した」と報告しない。
- ツールが画像ファイルまたは生成結果を返したことを確認するまで、画像生成成功として扱わない。
- 通常運用モードでは、画像が採用可能と確認できたらローカル転送、HTML反映、Validator、commit / push、公開確認へ続ける。セクション検証モードでは§5の結果報告後に停止する。

### 既存画像の部分修正

採用候補画像が既にあり、ユーザーが吹き出し、文字、表情、小物など一部分だけの修正を求めた場合は、新規生成ではなく既存画像の編集を優先する。

- 修正対象画像を明示的に編集元として使用する。
- 指定されていないキャラクターデザイン、構図、背景、服装、色、他の台詞を変更しない。
- 新規生成へ戻すのは、ユーザーが明示した場合、または画像編集が成立しない場合だけとする。
- 部分修正後も、ろひ・ルミナのキャラクター同一性が維持されているか確認する。

### 画像生成後の採用チェック

生成ファイルが返っただけでは採用完了としない。最低限、次を目視確認する。

1. 4コマがちょうど4パネルで構成されている
2. ろひとルミナの外見が固定デザインから大きく逸脱していない
3. 台詞本文へ `ろひ:` / `ルミナ:` などのト書き・話者名が描画されていない
4. 吹き出しの尻尾が正しい話者を指している
5. 日本語が大きく欠落・文字化け・意味不明化していない
6. 台詞が別キャラクターへ入れ替わっていない
7. 通常回ではその日の実装事実、機能紹介回では確認済みの現行機能事実と異なる数値・事故・機能が追加されていない

採用チェックに失敗した場合は、問題箇所だけを修正する編集を優先する。

### 画像生成失敗時の再試行

- 1回目の画像生成エラーだけで開発日記全体を停止しない。
- 最初に、使用ツール、生成指示、参照画像、出力条件が正しいかを確認し、呼び出しミスがあれば修正して直ちに再試行する。
- 正しい `image_gen.text2im` 呼び出しで最低1回は再試行する。必要に応じて、文字量や構図を簡略化した再試行を追加できる。
- 呼び出し自体が成立していない場合は「画像生成モデルが失敗した」と断定しない。まず呼び出し設定の不備として扱う。
- 正しい条件で再試行しても生成できない場合のみ未完了として停止し、使用したツール、試行回数、確認できたエラー内容を具体的に報告する。

掲載画像:

- 元画像の縦横比を維持する。
- 掲載時の文字可読性を最優先する。縮小後に4コマ内の日本語が読みにくくなる場合は無理に縮小しない。
- 十分な解像度を維持できる場合は生成画像の縦横を約1/2へ縮小してよい。
- 標準形式は高品質 WebP、quality 90 前後とするが、文字品質や変換品質、元画像サイズを考慮してPNG/JPEGをそのまま採用してもよい。
- PNG/JPEGを使う場合も過圧縮しない。
- HTML の `width` / `height` は実画像寸法に合わせる。
- CSS は `max-width: 100%; height: auto;` を維持する。

## 6. 画像をローカルへ渡す手順

正式経路は DevSpace の native artifact download を第一選択とし、Base64 ImageBridge は fallback とする。

### 画像転送ガード（必須）

native artifact download の前後では、共有ガードを必ず使用する。

```text
H:\codexapp\AgentTools\devlog-image-guard\Devlog-ImageTransferGuard.bat
```

このガードが、当日画像の正式保存先を次の1パスへ固定する。

```text
docs/assets/images/devlog-YYYY-MM-DD-comic.<ext>
```

呼び出し側は保存先を自分で組み立てない。ガードが返した `DESTINATION` をそのまま `download_artifact` へ渡す。`-final`、`-verify`、`-retry`、`ERROR`、`STOP` 等の別名を作ってはならない。

転送前:

```bat
H:\codexapp\AgentTools\devlog-image-guard\Devlog-ImageTransferGuard.bat -Mode prepare -WorkspaceRoot "H:\codexapp\vr-avatar-viewer-site" -Date YYYY-MM-DD -Extension png
```

判定:

- `ACTION=TRANSFER_ALLOWED`: 返された `DESTINATION` へ **1回だけ** `download_artifact` を実行する。
- `ACTION=DO_NOT_TRANSFER`: 正式画像が既に存在する。`download_artifact` を呼ばず、`verify` へ進む。
- `ACTION=STOP`: 転送しない。エラー内容を報告し、原因を解消するまで停止する。

転送後:

```bat
H:\codexapp\AgentTools\devlog-image-guard\Devlog-ImageTransferGuard.bat -Mode verify -WorkspaceRoot "H:\codexapp\vr-avatar-viewer-site" -Date YYYY-MM-DD -Extension png
```

`ACTION=TRANSFER_COMPLETE` で、サイズ、SHA-256、画像寸法を確認して§6のnative転送完了とする。以後、その日付の4コマについて `download_artifact` を再実行しない。

`Artifact destination already exists` が返った場合も別名へ変更して再転送しない。ガードの `prepare` / `verify` を再実行し、既存正式画像の状態を判定する。

### A. DevSpace native artifact download（標準）

1. ChatGPTで採用する4コマ画像を生成する。
2. 生成結果が native file として取得できることを確認する。
3. 対象サイトを `open_workspace` し、DevSpace の `download_artifact` で未使用のリポジトリ相対パスへ保存する。
4. ローカルで画像形式、実寸、ファイルサイズ、SHA-256を確認する。
5. 必要な場合だけ掲載用に縮小・WebP化し、`docs/assets/images`へ配置する。文字可読性が落ちる場合は元のPNG/JPEGを維持する。
6. 記事HTMLは、今回ChatGPTで生成し native artifact download で受信した正式画像、またはfallbackで正常復元できた正式画像を参照する。

画像転送のためだけに Discord へ一旦投稿しない。signed URL や ChatGPT 側の `/mnt/data` パスをローカルファイル扱いしない。

### B. Base64 ImageBridge 経由（fallback）

native artifact download が利用できない、無効、または転送に失敗した場合だけ使用する。

- 共通ImageBridge正本: `H:\codexapp\AgentTools\image-bridge`
- Base64 fallback の受信には共有側の `Receive-ChatGPTImage.bat` を使用し、サイトリポジトリ内に画像ブリッジの互換スクリプトを複製しない。

転送IDは1回の転送で固定する。途中で別IDへ分割しない。
WebP は ChatGPT 側で掲載サイズへ整えて `-Scale 1`、PNG/JPEG をローカルで縮小する場合だけ必要な `-Scale` を指定する。文字が読みにくくなるため、機械的に `-Scale 0.5` を強制しない。
復元後はファイルサイズとSHA-256を確認し、不完全なBase64を公開しない。

### C. Discord 経由（例外）

Discord Bot は開発連絡やユーザーが明示した画像投稿には引き続き使用できるが、ChatGPT生成画像のローカル受け渡し用途では通常使用しない。ImageBridge障害の原因を確認し、native artifact / Base64 fallbackの双方が利用できない場合にだけ代替経路として検討する。

## 7. 記事作成

既存記事のHTML構造を踏襲し、当日記事を `docs/devlog/YYYY-MM-DD.html` として作る。

記事の共通構成:

1. 日付 / Development Log
2. タイトル
3. リード文
4. 記事種別と根拠が分かる notice
5. 4コマ画像
6. X共有ボタン
7. 主題の導入
8. 本文
9. 技術的なポイント
10. 補足
11. 4コマの裏話
12. まとめ
13. 本日のひとこと

`development` では、本文に「何を実装・修正したか」「なぜ難しかったか」「同じ集計期間に進んだその他の変更」を含める。

`feature-spotlight` では、本文を「この機能で何ができるか」「どんな場面で便利か」「どういう仕組み・制約があるか」を中心に構成する。当日の小さな差分を機能の新規実装として書かない。

機能紹介回の notice では、前日と比べて有意な開発進展が小さいため閑話休題へ切り替えたことと、紹介内容が現行実装で確認できた機能に基づくことを明記する。

記事 `<head>` には §3 で決めた `devlog:type` を入れる。機能紹介回では `devlog:feature` も必須とする。

X共有対応:

- 各記事は絶対URLの `canonical`、Open Graph、`twitter:card=summary_large_image` を持つ。
- `og:image` / `twitter:image` は、その記事で実際に掲載している正式4コマ画像を指す。
- 4コマ画像の直下に `https://x.com/intent/tweet` を使った「Xで共有」ボタンを置く。
- Intentの本文には記事タイトルを明示的に含め、記事URL、`VRAvatarStudio` / `VRChat` の2つのハッシュタグだけを渡す。
- Web Intentから画像ファイル自体を直接アップロードしようとせず、記事URLのX Cardで4コマ画像を展開させる。

専門用語は一般向けの説明を先に書き、必要なら括弧内で VAB / VBG / VAC / PhysBone などの名称を補足する。

## 8. 一覧とトップページ更新

当日記事を作成したら同じ作業で以下を更新する。

- `docs/devlog/index.html`: 当日記事を先頭へ追加
- `docs/index.html`: 「最新の開発日記」を当日記事へ変更

`docs/devlog/index.html` に掲載するタイトル・日付は記事本文と一致させ、概要は記事本文の内容と矛盾しない要約にする。`docs/index.html` は「最新の開発日記」のリンク先が当日記事を指していればよく、トップページへ日付・タイトル・概要を重複掲載する必要はない。

## 9. 公開前ゲート

次をすべて満たすまで commit / push しない。

1. 当日記事ファイルが存在する
2. 4コマ画像ファイルが存在する
3. `__DEVLOG_IMAGE_DATA__` などのプレースホルダーが残っていない
4. 記事の画像 `src` が実在ファイルを指す
5. 日記一覧から当日記事へリンクされている
6. トップページから当日記事へリンクされている
7. 記事本文と日記一覧のタイトル・日付が一致し、概要の内容に矛盾がない
8. `git diff --check` がエラーなし
9. 意図しないファイル、秘密情報、巨大な一時ファイルが差分へ入っていない
10. 4コマの内容が、通常回ではその日の実装事実、機能紹介回では確認済みの現行機能事実と一致する
11. 機能紹介回では `devlog:type=feature-spotlight` と `devlog:feature` があり、同じ `devlog:feature` の過去機能紹介回がない

検証に1つでも失敗した場合は「未完成」として止め、成功扱いしない。

## 10. commit / push / GitHub Pages確認

公開作業では日記関連差分のみを確認し、意図した変更を commit して `main` へ push する。

Git操作は共通 Git Helper を使用し、原則として次の順で行う。

1. `git status --short` と `git diff --check` を確認する。
2. commit対象を当日の日記関連ファイルへ限定する。原則 `-All` は使わず `-Paths` で明示する。
3. Git Helper の `-DryRun` で `git add` / commit対象を確認する。
4. dry-run結果に意図しないファイルがないことを確認してから commit する。
5. ユーザーからpushの明示許可がある場合だけ `-Push` で `origin/main` へ push する。
6. push後にローカルHEADと `origin/main` が一致すること、作業ツリーに意図しない残差分がないことを確認する。

push 後の GitHub Pages 確認は検索エンジンのインデックスを使わず、公開URLへ直接HTTPリクエストして確認する。最低限、次をすべて確認する。

1. トップページURLが HTTP 200 で、当日記事へリンクしている
2. 当日記事URLが HTTP 200
3. 4コマ画像URLが HTTP 200
4. 公開記事に当日の日付とタイトルが表示される
5. 公開CSSで画像が `max-width: 100%; height: auto;` 等の縦横比維持指定になっている

GitHub Pages の反映待ちの場合は再確認する。検索結果に新記事が出ないことだけを公開失敗とは判定しない。公開URLを直接確認できないまま「公開済み」と報告しない。

## 11. 最終報告

必ず以下を報告する。

- 作業日付（JST）
- 集計期間
- VRAS - VR AVATAR STUDIO - の最終 HEAD
- 記事種別（`development` / `feature-spotlight`）
- 日記タイトル
- 掲載した4コマ画像
- サイト側 commit ID
- 公開URL
- 公開確認結果
- 主な開発内容
- 未完了事項があればその内容

## 12. 失敗時の扱い

セクション実行中にエラーやリトライが発生した場合、最終報告まで隠さず、その時点で次を報告する。

- 何が失敗したか
- 確認できたエラー内容
- 何を変更して再試行するか

単なる補助確認の失敗と、本処理そのものの失敗は区別して報告する。

- 画像生成失敗: 正規の `image_gen.text2im`、会話上の具体的な生成指示、実際に視覚参照可能な参照画像を確認して最低1回再試行する。再試行後も失敗した場合だけ記事公開を止め、呼び出しミスと生成サービス側エラーを区別して報告する。
- 画像転送失敗: native artifact download の失敗内容を確認し、Base64 ImageBridge fallbackを試す。壊れた画像やプレースホルダーのまま公開しない。
- DevSpace接続失敗: 1回再接続し、それでも失敗なら未完了として報告する。
- Git操作失敗: commit対象、既存stage、ブランチ、Git Helper dry-run結果を確認してから再試行する。意図しない差分を巻き込むために `-All` や既存stage混在を安易に許可しない。
- GitHub Pages未反映: 公開済み扱いにしない。検索エンジンではなく公開URLへの直接HTTP確認を優先する。
- 実装内容が確定できない: 推測で記事を書かない。

「一部できた」ことと「日記公開完了」は明確に分けて扱う。
